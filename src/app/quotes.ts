export class Quotes {
    constructor(public id: number, public submitter: string, public quote: string, public author: string, public date: Date, public upvote: number, public downvote: number) { }
}
